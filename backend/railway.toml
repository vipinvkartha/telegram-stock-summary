[build]
builder = "DOCKERFILE"

[deploy]
preDeployCommand = "alembic upgrade head"
startCommand = "sh -c 'uvicorn app.main:create_app --factory --host 0.0.0.0 --port ${PORT:-8000}'"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ALWAYS"
